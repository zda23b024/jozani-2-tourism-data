PGHOST=localhost
PGPORT=5432
PGDATABASE=booking_reviews_prod
PGUSER=postgres
PGPASSWORD=change_this_password

# Optional: use this instead of the PGHOST/PGDATABASE/PGUSER/PGPASSWORD fields.
# DATABASE_URL=postgresql://postgres:change_this_password@localhost:5432/booking_reviews_prod
